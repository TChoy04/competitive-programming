class Solution {
public:
    int findMin(vector<int>& nums) {
        int L = 0;
        int R = nums.size()-1;
        while(R>L){
            int mid = L + (R-L)/2;
            if(nums[mid]>nums[R]) L = mid+1;
            else R = mid;
        }
        return nums[L];
    }
};
