#include <bits/stdc++.h>
using namespace std;
#define ll long long
 
int main(){
	ll tc;
	cin>>tc;
	while(tc--){
		ll pile1, pile2;
		cin>>pile1>>pile2;
		if((pile1+pile2)%3==0 && 2*pile1>=pile2 &&2*pile2>=pile1) cout<<"YES";
		else cout<<"NO";
		cout<<endl;
	}

	return 0;
	}
