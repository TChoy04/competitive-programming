#include <bits/stdc++.h>
using namespace std;
void setIO(string s) {
	freopen((s + ".in").c_str(), "r", stdin);
	freopen((s + ".out").c_str(), "w", stdout);
}
 
int main(){
	//setIO("problemname");
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int tc;
	scanf("%i",&tc);
	while(tc--){
		
	}
	return 0;
}
